# Legacy

`aikido-gate.sh` is the original Bash implementation of the container gate
this project replaces. It is kept as the functional specification of the
historical process — it is not maintained, not part of the release, and not
a pattern for the Go architecture.
